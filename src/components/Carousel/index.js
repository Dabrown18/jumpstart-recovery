import React from 'react';
import ImageCarousel from './ImageCarousel';
import './Carousel.css'

const Carousel = () => (
	<div className="damage-instances">
		<h2>Money when you need it the most</h2>
		<ImageCarousel/>
	</div>
);

export default Carousel;